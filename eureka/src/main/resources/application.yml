logging:
  level: debug

---
spring:
  profiles: cks
server:
  port: 8761

eureka:
  instance:
    hostname: cks
  client:
#    registerWithEureka: true
#    fetchRegistry: true
    serviceUrl:
      defaultZone: http://cks1:8762/eureka/

---
spring:
  profiles: cks1
server:
  port: 8762
eureka:
  instance:
    hostname: cks1
  client:
    serviceUrl:
      defaultZone: http://cks:8761/eureka/
